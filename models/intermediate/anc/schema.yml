version: 2

models:
  - name: anc_visit_normalized
    description: "Normalized data of ANC visit" 
    +schema: intermediate
    columns: 
       - name: _airbyte_ab_id
         description: "The primary key for this table"
         tests:
           - unique
           - not_null

  - name: anc_case_normalized
    description: "Normalized ANC cases" 
    +schema: intermediate
    columns: 
       - name: _airbyte_ab_id
         description: "The primary key for this table"
         tests:
           - unique
           - not_null 

  - name: anc_visit_duplicates_removed
    description: "Deduped model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: anc_case_duplicates_removed
    description: "Deduped model for ANC cases"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null